"Incremental" feature for buildDevelopment.js where it watches the files and recompiles as necessary

Lazy loading, support for LABjs/control.js

buildDevelopment.js, accept paths instead of urls on the command line

.htc support

static urls in JavaScript should be treated as a relation

CDN

pngquant

manifest

support @import

"Check all paths" pre-commit hook mode

Draw dependency graph using graphviz or similar
